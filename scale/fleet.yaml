# Generated by generate.py -- do not edit by hand.
#
# The directory tree under repos/ IS the fleet: a directory holding a file is a
# repository, one holding only directories is a namespace. This file carries only
# the pinned git identity, which is what makes commit SHAs reproducible across
# machines and forges, and the topics a directory cannot express.
#
# Everything else stays at its default: private, on main, no tags, not archived.

version: 1

git:
  author:
    name: Forgelab Fixture
    email: fixture@forgelab.test
  timestamp: "2026-01-01T00:00:00Z"

defaults:
  visibility: private
  default_branch: main

repos:
  acme/catalog/api:
    topics: [python, catalog, github-actions]
  acme/catalog/docs:
    topics: [docs, catalog, no-ci]
  acme/catalog/events:
    topics: [java, catalog, jenkins]
  acme/catalog/importer:
    topics: [python, catalog, azure-pipelines]
  acme/catalog/media-pipeline:
    topics: [python, catalog, no-ci]
  acme/catalog/pricing:
    topics: [java, catalog, github-actions]
  acme/catalog/sdk:
    topics: [go, catalog, no-ci]
  acme/catalog/search-indexer:
    topics: [python, catalog, no-ci]
  acme/catalog/web:
    topics: [node, catalog, github-actions]
  acme/catalog/worker:
    topics: [python, catalog, azure-pipelines]
  acme/checkout/api:
    topics: [java, checkout, no-ci]
  acme/checkout/cart:
    topics: [node, checkout, github-actions]
  acme/checkout/cli:
    topics: [rust, checkout, no-ci]
  acme/checkout/docs:
    topics: [docs, checkout, no-ci]
  acme/checkout/events:
    topics: [go, checkout, no-ci]
  acme/checkout/infra:
    topics: [terraform, checkout, github-actions]
  acme/checkout/promo-engine:
    topics: [node, checkout, github-actions]
  acme/checkout/tax-engine:
    topics: [java, checkout, no-ci]
  acme/checkout/web:
    topics: [node, checkout, no-ci]
  acme/checkout/worker:
    topics: [go, checkout, azure-pipelines]
  acme/data/api:
    topics: [python, data, github-actions]
  acme/data/charts:
    topics: [helm, data, gitlab-ci]
  acme/data/docs:
    topics: [docs, data, github-actions]
  acme/data/ingest/connectors:
    topics: [java, data, gitlab-ci]
  acme/data/ingest/scheduler:
    topics: [python, data, github-actions]
  acme/data/lineage:
    topics: [python, data, azure-pipelines]
  acme/data/metrics-store:
    topics: [java, data, jenkins]
  acme/data/notebooks:
    topics: [docs, data, no-ci]
  acme/data/warehouse-models:
    topics: [python, data, gitlab-ci]
  acme/data/worker:
    topics: [python, data, jenkins]
  acme/design-system:
    topics: [docs, unowned, no-ci]
  acme/growth/api:
    topics: [ruby, growth, jenkins]
  acme/growth/email-templates:
    topics: [node, growth, gitlab-ci]
  acme/growth/experiments:
    topics: [node, growth, gitlab-ci]
  acme/growth/landing-pages:
    topics: [node, growth, github-actions]
  acme/growth/referrals:
    topics: [php, growth, github-actions]
  acme/growth/sdk:
    topics: [php, growth, github-actions]
  acme/growth/web:
    topics: [node, growth, no-ci]
  acme/growth/worker:
    topics: [php, growth, gitlab-ci]
  acme/handbook:
    topics: [node, unowned, azure-pipelines]
  acme/identity/api:
    topics: [java, identity, azure-pipelines]
  acme/identity/charts:
    topics: [helm, identity, jenkins]
  acme/identity/directory-sync:
    topics: [node, identity, github-actions]
  acme/identity/docs:
    topics: [docs, identity, no-ci]
  acme/identity/mfa:
    topics: [java, identity, github-actions]
  acme/identity/scim-sync:
    topics: [python, identity, github-actions]
  acme/identity/sdk:
    topics: [java, identity, no-ci]
  acme/identity/session-store:
    topics: [java, identity, azure-pipelines]
  acme/identity/sso-broker:
    topics: [python, identity, github-actions]
  acme/identity/web:
    topics: [node, identity, github-actions]
  acme/identity/worker:
    topics: [go, identity, github-actions]
  acme/logistics/api:
    topics: [python, logistics, azure-pipelines]
  acme/logistics/backoffice:
    topics: [node, logistics, github-actions]
  acme/logistics/carrier-adapters:
    topics: [go, logistics, github-actions]
  acme/logistics/events:
    topics: [dotnet, logistics, github-actions]
  acme/logistics/infra:
    topics: [terraform, logistics, azure-pipelines]
  acme/logistics/label-printer:
    topics: [go, logistics, no-ci]
  acme/logistics/tracking:
    topics: [go, logistics, no-ci]
  acme/logistics/web:
    topics: [node, logistics, github-actions]
  acme/logistics/worker:
    topics: [go, logistics, github-actions]
  acme/mobile/android-app:
    topics: [java, mobile, github-actions]
  acme/mobile/docs:
    topics: [docs, mobile, no-ci]
  acme/mobile/ios-app:
    topics: [shell, mobile, no-ci]
  acme/mobile/push-relay:
    topics: [shell, mobile, github-actions]
  acme/mobile/sdk:
    topics: [shell, mobile, jenkins]
  acme/mobile/web:
    topics: [node, mobile, no-ci]
  acme/payments/api:
    topics: [java, payments, azure-pipelines]
  acme/payments/docs:
    topics: [docs, payments, no-ci]
  acme/payments/fraud-scoring:
    topics: [go, payments, azure-pipelines]
  acme/payments/gateway/adapters:
    topics: [python, payments, azure-pipelines]
  acme/payments/gateway/api:
    topics: [go, payments, github-actions]
  acme/payments/gateway/worker:
    topics: [java, payments, jenkins]
  acme/payments/infra:
    topics: [terraform, payments, gitlab-ci]
  acme/payments/ledger:
    topics: [java, payments, github-actions]
  acme/payments/payouts:
    topics: [java, payments, github-actions]
  acme/payments/reconciliation:
    topics: [java, payments, github-actions]
  acme/payments/sdk:
    topics: [python, payments, jenkins]
  acme/payments/settlement:
    topics: [go, payments, azure-pipelines]
  acme/payments/worker:
    topics: [java, payments, github-actions]
  acme/platform/api:
    topics: [go, platform, github-actions]
  acme/platform/backoffice:
    topics: [node, platform, github-actions]
  acme/platform/build-cache:
    topics: [python, platform, github-actions]
  acme/platform/charts:
    topics: [helm, platform, jenkins]
  acme/platform/cli:
    topics: [go, platform, no-ci]
  acme/platform/docs:
    topics: [docs, platform, no-ci]
  acme/platform/feature-flags:
    topics: [go, platform, github-actions]
  acme/platform/infra:
    topics: [terraform, platform, jenkins]
  acme/platform/observability/alerting:
    topics: [go, platform, gitlab-ci]
  acme/platform/observability/collector:
    topics: [rust, platform, no-ci]
  acme/platform/observability/dashboards:
    topics: [node, platform, github-actions]
  acme/platform/service-catalog:
    topics: [go, platform, no-ci]
  acme/platform/worker:
    topics: [rust, platform, azure-pipelines]
  acme/security/api:
    topics: [shell, security, no-ci]
  acme/security/backoffice:
    topics: [node, security, github-actions]
  acme/security/docs:
    topics: [docs, security, gitlab-ci]
  acme/security/policy-bundles:
    topics: [shell, security, github-actions]
  acme/security/secret-rotator:
    topics: [shell, security, no-ci]
  acme/security/vuln-triage:
    topics: [go, security, github-actions]
  acme/security/worker:
    topics: [python, security, jenkins]
  acme/sre/cli:
    topics: [shell, sre, github-actions]
  acme/sre/infra:
    topics: [terraform, sre, jenkins]
  acme/sre/legacy-deploy:
    topics: [shell, sre, no-ci]
  acme/sre/runbooks:
    topics: [docs, sre, no-ci]
  acme/support/api:
    topics: [ruby, support, jenkins]
  acme/support/docs:
    topics: [docs, support, no-ci]
  acme/support/helpdesk-sync:
    topics: [php, support, github-actions]
  acme/support/knowledge-base:
    topics: [docs, support, no-ci]
  acme/support/macros:
    topics: [docs, support, no-ci]
